# Playlist Backup

## Setup

- Install `youtube-dlp` and add it to your path
- Add the `.env` variables
- Run `npm i`
- Run `npx prisma generate` and `npx prisma dev migrate --name init`
- Run `npm run start:dev`
